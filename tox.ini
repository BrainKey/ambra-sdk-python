[tox]
isolated_build = True
envlist = py310,py311,py312

[testenv]
deps = pytest
       dynaconf
       pytest-cov
       pytest-asyncio
       aioresponses
       requests-mock
       pytz

commands = pytest